/**
 * Centralized runtime configuration for the React app.
 * Add new app-wide variables here as they're introduced.
 */
export const env = {
  apiBaseUrl: 'https://gi3cnnvbv3.execute-api.ap-south-1.amazonaws.com/dev',
};
